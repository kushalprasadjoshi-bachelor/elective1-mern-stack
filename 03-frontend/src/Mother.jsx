// Use rafce for boilerplate
import React from "react";

const Mother = ({name, age}) => {
  return <div>
    <h1>Mpther</h1>
    Hello! I'm your mother speaking.
    My name is {name} and age is {age}.
  </div>;
};

export default Mother;
